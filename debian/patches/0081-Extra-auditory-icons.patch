From: "Igor B. Poretsky" <poretsky@rakurs.com>
Date: Thu, 24 Nov 2022 09:26:49 +0300
Subject: Extra auditory icons

---
 lisp/emacspeak-cperl.el    | 2 ++
 lisp/emacspeak-metapost.el | 5 +++++
 lisp/emacspeak-tcl.el      | 1 +
 3 files changed, 8 insertions(+)

diff --git a/lisp/emacspeak-cperl.el b/lisp/emacspeak-cperl.el
index 486f89b..578e290 100644
--- a/lisp/emacspeak-cperl.el
+++ b/lisp/emacspeak-cperl.el
@@ -147,6 +147,7 @@ Otherwise cue user to the line just created. "
   "Provide spoken feedback."
   (when (ems-interactive-p)
     (let ((prefix-arg (ad-get-arg 2)))
+      (emacspeak-auditory-icon 'section)
       (message "%s region containing %s lines"
                (if (and prefix-arg
                         (< prefix-arg 0))
@@ -158,6 +159,7 @@ Otherwise cue user to the line just created. "
   "Provide spoken feedback."
   (when (ems-interactive-p)
     (let ((prefix-arg (ad-get-arg 2)))
+      (emacspeak-auditory-icon 'section)
       (message "%s region containing %s lines"
                (if (and prefix-arg
                         (< prefix-arg 0))
diff --git a/lisp/emacspeak-metapost.el b/lisp/emacspeak-metapost.el
index 860e5ac..b503d43 100644
--- a/lisp/emacspeak-metapost.el
+++ b/lisp/emacspeak-metapost.el
@@ -73,6 +73,7 @@
 (defadvice meta-indent-line (after emacspeak pre act comp)
   "Provide auditory feedback."
   (when (ems-interactive-p)
+    (emacspeak-auditory-icon 'large-movement)
     (emacspeak-speak-line)))
 
 (defadvice meta-fill-paragraph (after emacspeak pre act)
@@ -102,6 +103,7 @@
   "Provide spoken feedback."
   (when (ems-interactive-p)
     (let ((prefix-arg (ad-get-arg 2)))
+      (emacspeak-auditory-icon 'section)
       (message "%s region containing %s lines"
                (if (and prefix-arg
                         (< prefix-arg 0))
@@ -113,6 +115,7 @@
   "Provide spoken feedback."
   (when (ems-interactive-p)
     (let ((prefix-arg (ad-get-arg 2)))
+      (emacspeak-auditory-icon 'section)
       (message "%s environment containing %s lines"
                (if  prefix-arg
                    "Uncommented"
@@ -122,12 +125,14 @@
 (defadvice meta-uncomment-defun (after emacspeak pre act)
   "Provide spoken feedback."
   (when (ems-interactive-p)
+    (emacspeak-auditory-icon 'section)
     (message "Uncommented environment containing %s lines"
              (count-lines (point) (mark 'force)))))
 
 (defadvice meta-uncomment-region (after emacspeak pre act)
   "Provide spoken feedback."
   (when (ems-interactive-p)
+    (emacspeak-auditory-icon 'section)
     (message "Uncommented  region containing %s lines"
              (count-lines (point) (mark 'force)))))
 
diff --git a/lisp/emacspeak-tcl.el b/lisp/emacspeak-tcl.el
index 28ad6a6..00ee55f 100644
--- a/lisp/emacspeak-tcl.el
+++ b/lisp/emacspeak-tcl.el
@@ -195,6 +195,7 @@ is a Tcl expression, and the last argument is Tcl commands.")
 (defadvice tcl-indent-line (after emacspeak pre act)
   "Speak the line"
   (when (ems-interactive-p)
+    (emacspeak-auditory-icon 'fill-object)
     (emacspeak-speak-line)))
 ;;}}}
 
