From: "Igor B. Poretsky" <poretsky@mlbox.ru>
Date: Sun, 27 Nov 2022 13:45:23 +0300
Subject: Original names instead of aliases

---
 lisp/emacspeak-imenu.el | 2 +-
 lisp/emacspeak-ocr.el   | 2 +-
 lisp/emacspeak-org.el   | 2 +-
 3 files changed, 3 insertions(+), 3 deletions(-)

diff --git a/lisp/emacspeak-imenu.el b/lisp/emacspeak-imenu.el
index 76ca0a7..0fae019 100644
--- a/lisp/emacspeak-imenu.el
+++ b/lisp/emacspeak-imenu.el
@@ -65,7 +65,7 @@
   ;; Third argument PREFIX is for internal use only.
 
   (cl-declare (special imenu-level-separator))
-  (mapcan
+  (cl-mapcan
    (function
     (lambda (item)
       (let* ((name (car item))
diff --git a/lisp/emacspeak-ocr.el b/lisp/emacspeak-ocr.el
index b187349..72b3f2a 100644
--- a/lisp/emacspeak-ocr.el
+++ b/lisp/emacspeak-ocr.el
@@ -453,7 +453,7 @@ The scanned image is converted to JPEG."
                (ems-interactive-p))
       (setq metadata
             (read-from-minibuffer "Enter picture description: "))
-      (let ((tempfile (format "temp%s.jpg" (gensym))))
+      (let ((tempfile (format "temp%s.jpg" (cl-gensym))))
         (shell-command
          (format  "mv %s %s; %s -c '%s' %s > %s; rm -f %s"
                   jpg  tempfile
diff --git a/lisp/emacspeak-org.el b/lisp/emacspeak-org.el
index 4229615..e99b97f 100644
--- a/lisp/emacspeak-org.el
+++ b/lisp/emacspeak-org.el
@@ -633,7 +633,7 @@ and assign  letter `h' to a template that creates the hyperlink on capture."
 (defun org-eww-store-link ()
   "Store a link to a EWW buffer."
   (when (eq major-mode 'eww-mode)
-    (org-store-link-props
+    (org-link-store-props
      :type "eww"
      :link   (emacspeak-eww-current-url)
      :url (emacspeak-eww-current-url)
